extends layout

block content
	h1 Welcome to Jexpress

	h3 An opinionated API that you can use, just by writing models.

	- if (bootstrap_test == false) {
	h4 Hello
	- }
	h4	Tests

	#users_exist
		.test.pending	Test if user table exists
		.result
		.tip
	hr

	#models
		.test.models	Show models
		.result